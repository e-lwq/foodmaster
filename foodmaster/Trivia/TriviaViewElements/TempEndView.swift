
import SwiftUI

struct TempEndView: View {
    var body: some View {
        ZStack{
            Image("bg_color")
                .resizable()
                .ignoresSafeArea()
            
            VStack{
                
            }
        }
    }
}

struct TempEndView_Previews: PreviewProvider {
    static var previews: some View {
        TempEndView()
    }
}
